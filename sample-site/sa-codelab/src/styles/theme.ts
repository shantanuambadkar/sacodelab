import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f5f5f5", // Example primary color
    },
    secondary: {
      main: "#f5f5f5", // Example secondary color
    },
  },
  typography: {
    fontFamily: "Arizonia", // Example font
  },
});

export default theme;